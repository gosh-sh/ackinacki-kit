
[package]
name = "shared"
version.workspace = true
edition.workspace = true

[features]
default = ["tokio/full"]
wasm = ["dep:gloo-timers", "tokio/sync"]

[dependencies]
gloo-timers = { optional = true, version = "0.3", features = ["futures"] }
tokio = { workspace = true, optional = true }
